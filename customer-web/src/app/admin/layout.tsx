export const metadata = {
  title: 'Vingt Trios — Admin Portal',
  description: 'Internal secured staff dashboard for Vingt Trios custom clothing platform.',
};

export default function RootAdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
